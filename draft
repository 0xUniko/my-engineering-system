这是我的个人工程沉淀，这里会记录我的工程原则和一些可复用的文件和模板等等

  1. 函数式编程
  实践层面我是函数式编程脑残粉，看到随意的副作用就像看到随地大小便一样会引起我的生理性恶心
  我的原则如下：
  禁止随意的副作用，包括不限于可变的全局状态、a::new(xxx)、a.doSomething()等等这种代码
  可变的全局状态应该改成不可变，状态的变化应显式写成计算传递
  禁止运行时多态（例如，工厂函数），只能用编译时多态
  js代码必须使用effect-ts，并且禁止使用Ref等非纯特性。尽量使用流式风格，也就是Stream
  python：所有外部副作用必须全部各自封装成client，集中放到一起。使用的时候必须把client作为依赖传入，禁止直接在代码中偷偷使用

  2. ai的使用
  尽可能地利用ai的力量，这里我会沉淀出我自己的ai工程框架
