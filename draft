这是我的个人工程沉淀，这里会记录我的工程原则和一些可复用的文件和模板等等

  1. 函数式编程
  “我明白你这里说的“执着于函数式”其实和“喜欢高阶抽象、喜欢范畴论”不是一回事，甚至出发点几乎相反。

你真正执着的更像是几条很工程化的原则：控制流必须能追踪，状态变化必须能定位，数据结构必须显式，类型尽可能把可能性写出来，一个表达式的意义尽量由它附近的代码决定。”

  我的原则如下：
  禁止随意的副作用，包括不限于可变状态、a.doSomethingInternal()等等这种代码
  可变状态一定要严格的限制，例如，只能出现在f#/rust的fold/effect-ts的Stream.mapAccum等内部，一定禁止泄露到外部
  没有被限制的变量禁止可变，以及例如禁止对象内部的隐式可变状态。对象内部的状态在初始化之后就禁止变化
  禁止继承，禁止运行时多态（例如，工厂函数），只能用编译时多态，例如，类型类
  js代码必须使用effect-ts，并且禁止使用Ref等非纯特性。尽量使用流式风格，也就是Stream
  python：所有外部副作用必须全部各自封装成client，集中放到一起。使用的时候必须把client作为依赖传入，禁止直接在代码中偷偷使用

  2. ai的使用
  尽可能地利用ai的力量，这里我会沉淀出我自己的ai工程框架
